<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.test.mapper.UserMapper">

    <resultMap type="com.test.domain.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="getUserByUsername" resultType="com.test.domain.entity.User">
        select id,username, password from users where username = #{username}
    </select>

    <select id="getRolesByUsername" resultType="com.test.domain.entity.Authority">
        select username, authority from authority where username = #{username}
    </select>

</mapper>